# 给 AI 的续做提示词：睿抗 2026 CAIR 平安城市仿真

你现在接手的是 `/home/zcy/raicom_ws` 里的 ROS Noetic + Gazebo 平安城市仿真项目。请优先阅读本文件和 `/home/zcy/raicom_ws/任务完成报告.md`，按当前进度继续做，不要从零重写。

## 总目标

完成睿抗 2026 CAIR 平安城市仿真任务：在 Gazebo 中运行 4m x 4m 平安城市地图，小车自动巡检，摄像头看到墙面照片/目标后，调用真实识别模型输出结果，而不是用位置触发、照片模板比对或手写假结果。

终端需要能看到类似下面的识别输出：

```text
识别结果：垃圾桶类型=...
识别结果：人群类型=...，数量=...
识别结果：楼宇灾害情况=...
```

## 任务布置区

以后用户的新任务优先写在这里，后续 AI 接手时先看这一栏，再结合下面的项目状态执行。完成任务后，把已完成内容移动到“当前已经完成的状态”或“已验证过的结果”，并更新 `/home/zcy/raicom_ws/任务完成报告.md`。

当前待办：

```text
1. 暂无新的待办。继续任务时，把用户最新要求写到这里。
```

最近已处理：

```text
1. 2026-05-15：按“尽可能快但不明显偏离中线、摄像头仍能识别”的要求，提高中心线巡航速度。
   gui_demo.launch 中心线参数调整为 base_speed=0.220、corner_speed=0.130、lookahead=0.30、max_angular=1.80、heading_gain=2.85、cross_track_gain=0.55。
   同步修改 centerline_follower.py 默认值，避免单独启动脚本时还使用旧慢速参数。
   headless 实跑验证：45 秒 odom 监控最大中心线偏差约 0.054m，平均约 0.017m；终端输出垃圾桶、人群、楼宇检测到火灾，centerline_follower 正常完成一圈。
2. 2026-05-15：给相机识别节点修复楼宇提前输出问题。
   原因是楼宇识别曾经“第一次打印后永久去重”，并且没检测到楼宇框时会对整张图跑火灾模型，容易提前误报。
   现在改为目标离开后可重置、间隔可复报，并且楼宇结果必须先有足够大的楼宇框才输出。
3. 2026-05-15：给垃圾桶、人群、楼宇加入目标面积占比过滤，减少其他照片触发错误模型输出。
4. 2026-05-15：楼宇火灾保留真实 ONNX 模型推理，同时增加火焰颜色辅助提示，解决当前仿真楼宇火焰照片上 fire.onnx 火焰类别不稳定的问题。
```

## 当前已经完成的状态

1. 主包是 `/home/zcy/raicom_ws/src/raicom_safe_city`。
2. 默认启动入口是：

```bash
cd /home/zcy/raicom_ws
source devel/setup.bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true
```

3. 已经实现真实摄像头模型识别节点：

```text
/home/zcy/raicom_ws/src/raicom_safe_city/scripts/camera_model_recognition_node.py
```

该节点订阅：

```text
/camera/rgb/image_raw
```

4. 识别逻辑已经从“到某个位置后输出结果”改成“摄像头图像中实际看到目标后输出结果”。
5. 旧的位置触发识别节点 `proximity_recognition_node.py` 还在，但默认不启动。不要把它重新作为主方案。
6. 墙面照片只是 Gazebo 中给摄像头看的视觉目标，不允许再用模板相似度直接比对图片。
7. 当前默认用 GPU 跑 ONNXRuntime；CPU 只作为备用。
8. RViz 已做轻量化：默认关闭 TF、Global Costmap、Local Costmap 显示，Frame Rate 设为 10，避免 WSL 图形界面更卡。
9. Gazebo 相机帧率已恢复为 15Hz，识别节点默认 `process_hz=3.0`，`stable_frames=1`。
10. 识别节点已经不是“一类结果只打印一次”。现在目标离开视野若干帧后会重置，同一目标持续可见时也可按 `report_cooldown` 间隔复报。
11. 楼宇结果现在必须先检测到足够大的楼宇框，避免没走到楼宇时因为整张画面误检火灾而提前输出。
12. 垃圾桶、人群、楼宇都加了面积占比过滤，降低不同墙面照片之间的交叉误检。

## 当前使用的真实模型

模型来自 `/home/zcy/raicom_ws/2025源码/`：

```text
垃圾桶模型：/home/zcy/raicom_ws/2025源码/垃圾桶/moxing/best.onnx
人群模型：/home/zcy/raicom_ws/2025源码/人/human.onnx
楼宇模型：/home/zcy/raicom_ws/2025源码/楼宇火灾识别/building.onnx
火灾/楼层模型：/home/zcy/raicom_ws/2025源码/楼宇火灾识别/fire.onnx
OCR 模型目录：/home/zcy/raicom_ws/2025源码/楼宇火灾识别/OCR/
红绿灯模型：/home/zcy/raicom_ws/2025源码/红绿灯/moxing/best.onnx
```

当前默认只使用垃圾桶、人群、楼宇、火灾 ONNX 模型。OCR 默认关闭，因为 FastDeploy/Paddle OCR 在 ROS 图像回调中出现过段错误崩溃。红绿灯模型目前不是主任务输出要求，默认不启用。

## GPU/依赖现状

当前 Python 是 3.8。ONNXRuntime GPU 版本使用：

```text
onnxruntime-gpu==1.19.2
```

不要再强装 `onnxruntime-gpu==1.20.1`，因为 Python 3.8 和部分镜像源找不到这个版本。

用户已经安装了 CUDA 12 运行库、cuBLAS、cuDNN 9。为了让 ONNXRuntime GPU 能找到额外动态库，也安装了用户级 pip 库：

```text
nvidia-curand-cu12
nvidia-cufft-cu12
nvidia-nvjitlink-cu12
```

launch 文件里已经加入这些库路径：

```text
/home/zcy/.local/lib/python3.8/site-packages/nvidia/curand/lib
/home/zcy/.local/lib/python3.8/site-packages/nvidia/cufft/lib
/home/zcy/.local/lib/python3.8/site-packages/nvidia/nvjitlink/lib
```

判断 GPU 是否真的启用，看日志中是否出现：

```text
ONNXRuntime Providers: ['CUDAExecutionProvider', 'CPUExecutionProvider']
```

如果只看到 `CPUExecutionProvider`，说明 GPU 没有加载成功。

可单独检查：

```bash
python3 - <<'PY'
import onnxruntime as ort
print(ort.__version__)
print(ort.get_available_providers())
PY
```

## 常用命令

编译：

```bash
cd /home/zcy/raicom_ws
catkin_make
source devel/setup.bash
```

默认完整演示：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true
```

只启动相机识别：

```bash
roslaunch raicom_safe_city camera_recognition.launch
```

强制 CPU 备用启动：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true use_gpu:=false
```

不开 RViz/Gazebo GUI 的轻量测试：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=false gui:=false headless:=true
```

不让小车自动巡线：

```bash
roslaunch raicom_safe_city gui_demo.launch run_centerline_follower:=false
```

指定地图：

```bash
roslaunch raicom_safe_city gui_demo.launch map_file:=/home/zcy/raicom_ws/src/raicom_safe_city/maps/safe_city_map.yaml
```

建图：

```bash
roslaunch raicom_safe_city slam.launch
```

保存地图：

```bash
rosrun map_server map_saver -f /home/zcy/raicom_ws/src/raicom_safe_city/maps/safe_city_map
```

键盘控制：

```bash
rosrun teleop_twist_keyboard teleop_twist_keyboard.py cmd_vel:=/cmd_vel
```

## 关键文件

```text
src/raicom_safe_city/scripts/camera_model_recognition_node.py
src/raicom_safe_city/launch/gui_demo.launch
src/raicom_safe_city/launch/camera_recognition.launch
src/raicom_safe_city/launch/navigation.launch
src/raicom_safe_city/launch/gazebo.launch
src/raicom_safe_city/urdf/raicom_guard_rover.urdf.xacro
src/raicom_safe_city/config/navigation.rviz
src/raicom_safe_city/worlds/safe_city.world
src/raicom_safe_city/maps/safe_city_map.yaml
任务完成报告.md
```

## 已验证过的结果

1. `catkin_make` 通过。
2. launch XML 检查通过。
3. `camera_model_recognition_node.py` Python 编译通过。
4. ONNXRuntime GPU 可用，provider 是：

```text
['CUDAExecutionProvider', 'CPUExecutionProvider']
```

5. 用测试图片发布到相机 topic 时，识别节点能输出：

```text
识别结果：人群类型=普通行人，数量=1
识别结果：垃圾桶类型=厨余垃圾桶，状态=未投放
识别结果：楼宇灾害情况=楼宇2楼着火了
```

6. 单独测试仿真墙面 `crowd_people.jpg` 时，人群模型检测到 1 个人。之前看到 2 个人，是因为测试用的是 `2025源码/人/images/ro1.jpg`，不是仿真墙面那张图。
7. 完整 headless launch 已经能启动，日志里显示 CUDA provider，识别节点没有再像 FastDeploy OCR 那样崩溃。
8. 强制 timeout 结束 Gazebo 时可能出现 `boost::lock_error`，这是退出阶段的 Gazebo/ROS 关闭问题，不是识别节点崩溃。
9. 2026-05-15 实测三张仿真墙面图片：

```text
crowd_people.jpg       -> 只输出人群，不输出楼宇
trash_food_waste.jpg   -> 只输出垃圾桶，不输出楼宇/人群
building_fire.jpg      -> 只输出楼宇灾害情况
```

## 后续继续做时的重点

1. 先用默认命令启动 GUI demo，确认 RViz 的 Side Camera 能看到墙面照片，并且终端能输出三类识别结果。
2. 如果 RViz 卡顿，优先保持 TF、Global Costmap、Local Costmap 关闭；不要默认打开复杂显示层。WSL 下 Gazebo/RViz 即使用 GPU 渲染，也可能因为 WSL 图形转发、Gazebo GUI、RViz 显示层和 CPU 仿真线程共同占资源而卡。
3. 如果识别没有输出，依次检查：

```bash
rostopic hz /camera/rgb/image_raw
rosnode list | grep camera_model_recognition
rostopic echo /camera/model_recognition/result
```

同时看日志里是不是 CUDA provider，节点有没有 died。

4. 如果要提高识别帧率，优先调 `process_hz`，但不要让推理堵塞 ROS 图像回调。GPU 能提高推理速度，但摄像头 topic 帧率还受 Gazebo 相机插件、WSL 图形性能和系统负载限制。
5. 如果小车巡线转弯或看照片角度不好，调整 `centerline_follower` 参数和路线，不要把识别退回位置触发。
6. 修改代码后同步更新 `/home/zcy/raicom_ws/任务完成报告.md`。

## 明确不要做的事

1. 不要把 sudo 密码、账号密码、token 等敏感信息写入任何文件。
2. 不要再把主识别方案改回图片模板匹配。
3. 不要再把主识别方案改回位置触发。
4. 不要删除用户其他工作区，例如 `/home/zcy/drone_ws`。
5. 不要用 `git reset --hard`、大范围删除、覆盖用户文件等破坏性操作。
6. 不要为了“看起来有识别结果”写死假输出；必须尽量调用真实模型。
7. 不要默认启用 FastDeploy OCR；它之前导致过 ROS 节点段错误。

## 给后续 AI 的工作方式要求

接手后先读本文件和任务完成报告，再检查当前代码状态。需要改动时，小步修改、小步测试，并把关键命令和结果写回任务完成报告。最终回答用户时，重点告诉他用了什么命令、结果是什么、下一步怎么跑，不要写一大堆无关解释。
