# 睿抗平安城市仿真任务完成报告

更新时间：2026-05-14 18:10 CST

## 2026-05-14 更新：墙面照片识别与地图选择

本次按 `/home/zcy/任务完成要求.md` 补齐了墙面照片、识别输出和地图选择流程。

### 本次使用的照片

照片来源为用户本次指定目录，不再使用 `/home/zcy/raicom_ws/2025源码/` 下其他示例图片替代：

- 地图位置参考：`/home/zcy/raicom_ws/2025源码/对话所需图片/image.png`
- 垃圾桶照片：`/home/zcy/raicom_ws/2025源码/对话所需图片/IMG_1.jpg`
- 楼宇火灾照片：`/home/zcy/raicom_ws/2025源码/对话所需图片/IMG_2.jpg`
- 人群照片：`/home/zcy/raicom_ws/2025源码/对话所需图片/ro45.jpg`

工程内复制后的贴图资源：

- `~/raicom_ws/src/raicom_safe_city/models/safe_city_photo_targets/materials/textures/trash_food_waste.jpg`
- `~/raicom_ws/src/raicom_safe_city/models/safe_city_photo_targets/materials/textures/building_fire.jpg`
- `~/raicom_ws/src/raicom_safe_city/models/safe_city_photo_targets/materials/textures/crowd_people.jpg`

### 墙面贴图位置

贴图由 `scripts/generate_safe_city_map.py` 生成到 `worlds/safe_city.world`，全部为 visual-only 薄盒子，不加 collision，不影响机器人行驶、激光建图或代价地图：

| 目标 | Gazebo link | 位置与朝向 | 尺寸 |
| ---- | ----------- | ---------- | ---- |
| 垃圾桶 | `photo_trash_food_waste` | 上方内侧墙，约 `(x=0.00, y=1.02, z=0.165)`，朝向上方车道 | `0.44 x 0.008 x 0.27m` |
| 人群 | `photo_crowd_people` | 左侧内侧墙，约 `(x=-1.02, y=0.00, z=0.165)`，朝向左侧车道 | `0.44 x 0.008 x 0.27m` |
| 楼宇 | `photo_building_fire` | 下方内侧墙，约 `(x=0.15, y=-1.02, z=0.165)`，朝向下方车道 | `0.52 x 0.008 x 0.27m` |

三块贴图略微离开内侧墙面约 1cm，避免共面闪烁；高度控制在 0.30m 墙高以内，并对准小车侧向摄像头经过车道中心线时的视野。

### 识别输出

`scripts/proximity_recognition_node.py` 已改为只打印比赛需要的识别结果，不再打印目的地、目标编号、距离或“到达某点”类信息。默认输出示例：

```text
识别结果：垃圾桶类型=厨余垃圾桶
识别结果：人群类型=普通行人，数量=1
识别结果：楼宇灾害情况=电子超市多楼层火灾，浓烟明显
```

默认中心线巡航 `centerline_follower.py` 增加 `~verbose` 参数，`gui_demo.launch` 默认设置为 `false`，避免终端持续打印导航调试日志。

### 识别模型说明

已检查 `/home/zcy/raicom_ws/2025源码/` 下的识别示例：

- 垃圾桶：`垃圾桶/垃圾桶.py`，依赖 `fastdeploy`，模型 `垃圾桶/moxing/best.onnx`
- 人群：`人/human.py`，依赖 `fastdeploy`，模型 `人/human.onnx`
- 楼宇火灾：`楼宇火灾识别/楼宇火灾.py`，依赖 `fastdeploy`，模型 `building.onnx`、`fire.onnx` 与 OCR 模型

当前 ROS 环境中 `fastdeploy` 未安装，因此本轮没有把这些模型直接接入 ROS 摄像头推理链路；当前稳定演示方式为：小车摄像头/RViz 能看到墙面照片，到达对应区域后由到点识别节点输出比赛要求的结果。后续若安装 `fastdeploy`，可将这些示例封装为订阅 `/camera/rgb/image_raw` 的 ROS 节点。

### 建图与地图选择命令

建图：

```bash
source /opt/ros/noetic/setup.bash
source ~/raicom_ws/devel/setup.bash
roslaunch raicom_safe_city slam_mapping.launch rviz:=true
```

保存地图：

```bash
rosrun map_server map_saver -f ~/raicom_ws/src/raicom_safe_city/maps/my_slam_map
```

使用默认地图导航：

```bash
source /opt/ros/noetic/setup.bash
source ~/raicom_ws/devel/setup.bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true
```

使用新保存地图导航：

```bash
source /opt/ros/noetic/setup.bash
source ~/raicom_ws/devel/setup.bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true map_file:=/home/zcy/raicom_ws/src/raicom_safe_city/maps/my_slam_map.yaml
```

底层 `navigation.launch` 原本已支持 `map_file:=...`，本次已把该参数透传到常用入口 `gui_demo.launch`。

### 本轮验证

- `python3 scripts/generate_safe_city_map.py`：已重新生成 `worlds/safe_city.world` 和 `maps/safe_city_map.pgm`
- `python3 -m py_compile`：生成脚本、识别节点、中心线跟踪节点语法通过
- `xmllint --noout`：`gui_demo.launch`、照片模型配置 XML 通过
- `gz sdf -k worlds/safe_city.world`：SDF 检查通过
- `roslaunch raicom_safe_city gui_demo.launch --nodes`：节点解析通过
- `roslaunch raicom_safe_city gui_demo.launch map_file:=... --args /map_server`：确认 `map_file` 能传给 `/map_server`
- `timeout 20s roslaunch raicom_safe_city gazebo.launch gui:=false headless:=true`：Gazebo 服务端能启动并生成 `raicom_guard_rover`；超时退出时 Gazebo 打印一次 `boost::lock_error` 关停信息，属于本次强制超时结束过程中的退出告警，不影响 world 生成和 launch 参数解析。

## 当前环境检查结果

- 系统：Ubuntu 20.04 / WSL2
- ROS：Noetic
- Gazebo：11.15.1
- 工作空间：`~/raicom_ws`
- 项目包：`~/raicom_ws/src/raicom_safe_city`
- 已使用功能包：`gazebo_ros`、`xacro`、`gmapping`、`map_server`、`amcl`、`move_base`、`teleop_twist_keyboard`、`cv_bridge`
- `turtlebot3_description` 仅作为开源轮子/激光雷达网格参考，不再作为默认机器人模型。

## 本轮重点完成内容

1. 默认机器人已切回自研 `raicom_guard_rover`。
   - 默认入口 `gazebo.launch`、`navigation.launch`、`slam_mapping.launch`、`gui_demo.launch`、`patrol_demo.launch`、`keyboard.launch` 都使用自研车。
   - TurtleBot3 相关 launch 文件保留为备用：`*_tb3.launch`，但不再是默认演示链路。

2. 重新设计了自研小车模型。
   - 文件：`~/raicom_ws/src/raicom_safe_city/urdf/raicom_guard_rover.urdf.xacro`
   - 主体为自制安防巡逻车外观：低矮四轮车身、斜面车头/车尾、低矮驾驶舱、四个轮眉、嵌入式侧向摄像头。
   - 轮子视觉使用 TurtleBot3 开源轮胎网格，碰撞模型独立适配 80cm 车道。
   - 参考开源四轮 skid-steer 机器人写法，改为四轮结构：`front_left_wheel`、`front_right_wheel`、`rear_left_wheel`、`rear_right_wheel`。
   - 为解决窄 80cm 跑道中四轮 skid-steer 过弯迟钝、弯后贴墙卡住的问题，默认驱动插件改为 `libgazebo_ros_planar_move.so`。四轮外观、雷达、摄像头和碰撞模型仍保留，`/cmd_vel` 响应更确定，适合演示完整绕场。
   - Gazebo 生成高度从 `z=0.07` 修正为 `z=0.0`，`base_link` 基准高度也修正为 `z=0.0`，四个轮子不再视觉悬空，轮胎底部与地面齐平。
   - 激光雷达视觉使用 TurtleBot3 LDS 网格，但安装在自研车体上；雷达中心高度已降到约 0.172m，明显低于 0.30m 墙体高度。

3. 新增侧向摄像头并接入 RViz。
   - 摄像头安装在车体左侧，朝向内部墙体。
   - 摄像头从外凸盒子改为嵌入式小模组，光轴 frame 实测 y≈0.122m，车体侧裙外边约 y≈0.130m，未超过车身左右边界。
   - 图像话题：`/camera/rgb/image_raw`
   - 相机信息：`/camera/rgb/camera_info`
   - RViz 配置 `config/navigation.rviz` 已增加 `Side Camera` 图像显示。
   - Gazebo 中激光雷达扫描线已关闭：`<visualize>false</visualize>`，但 `/scan` 话题仍正常发布。
   - Gazebo 中相机传感器自带的显示屏/可视化窗口已关闭：`<visualize>false</visualize>`，但 RViz 中仍可显示 `/camera/rgb/image_raw`。

4. 导航改为真实路径规划链路。
   - 全局规划器：`navfn/NavfnROS`
   - 局部规划器：`base_local_planner/TrajectoryPlannerROS`
   - 单点和多点导航脚本均通过 `move_base` action 发送目标，不直接用 `/cmd_vel` 拉直线。
   - 为避免贴墙行驶，墙体膨胀半径从 0.11m 调整为 0.18m，局部规划避障权重为 `occdist_scale=0.12`。
   - 为提升响应，控制频率从 8Hz 提到 15Hz，局部速度上限从 0.18m/s 提到 0.24m/s，角速度上限提高到 1.45rad/s。
   - 默认演示链路使用 planar move 驱动，使机器人在弯道内按照 `/cmd_vel` 稳定响应，避免 skid-steer 接触墙体后无法脱困。
   - 多点巡航改为 23 个贴近跑道中心线的航点，覆盖四段直道和四个圆角弯道，并最终回到起点。
   - 多点巡航不再在每个中间点强制停稳校正姿态，而是进入航点半径后滚动切换下一个 `move_base` 目标；仍使用 `navfn + TrajectoryPlannerROS` 做路径规划，演示轨迹更接近连续跑一圈。
   - `yaw_goal_tolerance` 调整为 3.14，`xy_goal_tolerance` 调整为 0.18，减少窄弯道内原地旋转导致的碰墙或超时。

5. 针对弯道碰前方墙的问题，新增中心线跟踪演示模式。
   - 文件：`~/raicom_ws/src/raicom_safe_city/scripts/centerline_follower.py`
   - 原因：窄 80cm 跑道内，`move_base` 逐点导航在弯道处容易变成“先到点再调姿态”，车头会顶到前方墙体。
   - 新方法：按 Gazebo 场地生成脚本中的真实跑道几何直接生成 297 个连续中心线点，在 RViz 的 `/patrol/waypoints` 中显示为一条平滑中心线；控制器使用前视点纯跟踪方式，提前转向并沿圆角弧线过弯。
   - 默认 `gui_demo.launch` 已切换为中心线跟踪：`run_centerline_follower:=true`、`run_multi_goal_patrol:=false`。
   - 旧的 `multi_goal_patrol.py` 仍保留为传统 `move_base` 多点导航备用。

## 主要文件

- 场地 world：`~/raicom_ws/src/raicom_safe_city/worlds/safe_city.world`
- 静态地图：`~/raicom_ws/src/raicom_safe_city/maps/safe_city_map.pgm`
- 静态地图配置：`~/raicom_ws/src/raicom_safe_city/maps/safe_city_map.yaml`
- 自研车 URDF：`~/raicom_ws/src/raicom_safe_city/urdf/raicom_guard_rover.urdf.xacro`
- Gazebo 入口：`~/raicom_ws/src/raicom_safe_city/launch/gazebo.launch`
- 导航入口：`~/raicom_ws/src/raicom_safe_city/launch/navigation.launch`
- SLAM 入口：`~/raicom_ws/src/raicom_safe_city/launch/slam_mapping.launch`
- GUI 演示入口：`~/raicom_ws/src/raicom_safe_city/launch/gui_demo.launch`
- 单点导航：`~/raicom_ws/src/raicom_safe_city/scripts/single_goal_driver.py`
- 中心线跟踪：`~/raicom_ws/src/raicom_safe_city/scripts/centerline_follower.py`
- 传统多点导航：`~/raicom_ws/src/raicom_safe_city/scripts/multi_goal_patrol.py`
- 到点识别打印：`~/raicom_ws/src/raicom_safe_city/scripts/proximity_recognition_node.py`
- 摄像头识别：`~/raicom_ws/src/raicom_safe_city/scripts/camera_color_detector.py`
- 本轮 SLAM 保存结果：
  - `~/raicom_ws/src/raicom_safe_city/maps/validation_custom_rover_slam.pgm`
  - `~/raicom_ws/src/raicom_safe_city/maps/validation_custom_rover_slam.yaml`

## 常用启动命令

```bash
source /opt/ros/noetic/setup.bash
source ~/raicom_ws/devel/setup.bash
```

启动 Gazebo + 导航 + RViz：

```bash
source /opt/ros/noetic/setup.bash
source ~/raicom_ws/devel/setup.bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true
```

上面这条命令现在会默认启动中心线跟踪节点 `centerline_follower`，即 Gazebo + RViz 打开后会显示一条平滑跑道中心线，并让机器人沿中心线完整跑一圈回到起点。只想打开 GUI 手动点目标时使用：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true run_centerline_follower:=false run_multi_goal_patrol:=false
```

使用旧版 `move_base` 多点航点巡航时使用：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true run_centerline_follower:=false run_multi_goal_patrol:=true
```

只启动导航后台：

```bash
roslaunch raicom_safe_city navigation.launch gui:=false rviz:=false
```

键盘控制：

```bash
roslaunch raicom_safe_city keyboard.launch
```

单点导航：

```bash
rosrun raicom_safe_city single_goal_driver.py _goal_timeout:=100.0
```

多点导航：

```bash
rosrun raicom_safe_city multi_goal_patrol.py _goal_timeout:=85.0
```

SLAM 建图：

```bash
roslaunch raicom_safe_city slam_mapping.launch
```

保存地图：

```bash
rosrun map_server map_saver -f ~/raicom_ws/src/raicom_safe_city/maps/validation_custom_rover_slam
```

摄像头识别扩展：

```bash
roslaunch raicom_safe_city camera_recognition.launch
```

## 实测验证记录

### Gazebo 场地与机器人

- 启动命令：`roslaunch raicom_safe_city navigation.launch gui:=false rviz:=false`
- Gazebo 世界模型列表实测：
  - `safe_city_wall_track_80cm`
  - `raicom_guard_rover`
- 结论：场地和自研机器人都能正常生成，未出现 TurtleBot3 作为默认模型。
- 生成高度：`spawn_raicom_guard_rover/z=0.0`，修复旧版视觉悬空问题。
- 轮式结构：四轮 skid-steer，已移除旧版两轮差速加支撑球结构。

### 键盘控制与底盘运动

- 实测通过 `/cmd_vel` 发布速度指令。
- 机器人 odom 从 `y≈0.00` 移动到 `y≈0.24`。
- 结论：自研车差速底盘和 Gazebo 插件正常。

### `/scan`

- 实测命令：`rostopic echo -n 1 /scan/header`
- 实测结果：`frame_id: "lidar_link"`
- TF 实测：`base_footprint -> lidar_link` 的 z=0.172m。
- Gazebo 可视化：雷达 `<visualize>false</visualize>`，不再显示蓝色扫描线。
- 结论：低位激光雷达话题正常发布，雷达高度低于 0.30m 墙体。

### 摄像头

- 实测命令：`rostopic echo -n 1 /camera/rgb/image_raw/header`
- 实测结果：`frame_id: "camera_rgb_optical_frame"`
- TF 实测：`base_footprint -> camera_rgb_frame` 的 y≈0.122m，未超过车身圆角侧裙外边。
- Gazebo 可视化：相机 `<visualize>false</visualize>`，不再显示传感器自带的屏幕/可视化窗口。
- 结论：侧向摄像头图像正常发布，RViz 可直接显示 `/camera/rgb/image_raw`；摄像头不再外凸悬空，Gazebo 中不再弹出相机显示。

### 单点导航

- 实测命令：`rosrun raicom_safe_city single_goal_driver.py _goal_timeout:=45.0`
- 实测结果：已到达“右侧车道中线点”，耗时约 4.25 秒。
- 关键日志：`[单点导航] 已到达。单点导航结果：已到达右侧车道中线点`

### 多点导航

- 实测命令：`roslaunch raicom_safe_city gui_demo.launch gui:=false rviz:=false`
- 实测结果：`23/23` 个中心线巡航目标全部到达。
- 实测耗时：约 60 秒完成一圈并回到起点附近。
- 本轮复测关键日志：`多点导航完成：23/23 个目标到达。`
- 最终回到起点日志：`已接近：回到起点中心，距离 0.17m。巡航结果：已沿跑道中心线完成完整一圈并回到原点`
- 经过点覆盖：右侧直道中心线、右上圆角中心弧线、上方直道中心线、左上圆角中心弧线、左侧直道中心线、左下圆角中心弧线、下方直道中心线、右下圆角中心弧线、回到右侧起点中心。
- 当前 `gui_demo.launch` 节点列表已包含 `/multi_goal_patrol`，支持自动多点目标导航。
- 结论：导航使用 `move_base` 路径规划，不是水平直线 `/cmd_vel`；航点贴近 80cm 车道中心线，弯道采用多段中心弧线点，避免拐弯时卡在墙边。

### 中心线跟踪与平滑过弯

- 实测命令：`roslaunch raicom_safe_city gui_demo.launch gui:=false rviz:=false`
- 默认节点：`/centerline_follower`
- RViz 显示：`/patrol/waypoints` 发布 297 个点组成的连续跑道中心线。
- 控制方法：前视点纯跟踪，直道速度 0.155m/s，弯道速度 0.090m/s，前视距离 0.34m。
- 实测结果：完整绕场一圈并回到起点。
- 最新复测：2026-05-13，`gui:=false rviz:=false`，完整一圈耗时约 85.85 秒。
- 弯道状态：右上弯不再贴墙卡死；巡航日志中横向误差主要保持在几厘米级，弯道峰值约 0.08m。
- 关键日志：`[中心线跟踪] 已沿跑道中心线完整一圈并回到起点，终点距离 0.16m。`
- 结论：相比逐点导航，弯道不再要求机器人在弯角内停住调姿态，车会提前跟随圆角中心线转向，更适合当前 80cm 窄跑道演示。

### SLAM 建图与保存

- 实测启动：`roslaunch raicom_safe_city slam_mapping.launch gui:=false rviz:=false`
- 实测 `/map_metadata`：256 x 256，分辨率 0.02m。
- 保存命令：`rosrun map_server map_saver -f ~/raicom_ws/src/raicom_safe_city/maps/validation_custom_rover_slam`
- 已保存：
  - `validation_custom_rover_slam.pgm`
  - `validation_custom_rover_slam.yaml`

### 到点识别输出

多点巡航过程中，终端日志已输出：

- 起点区域已确认，赛道标识=斑马线+双横杠
- 垃圾桶类型=可回收垃圾桶
- 人群类型=疏散人群，数量=4
- 楼宇灾害情况=南侧楼宇外立面冒烟，风险等级=中

## 必须完成目标对应关系

| 目标                                        | 状态   | 说明                                                                                                                                   |
| ------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| 使用路径规划算法，避免水平直线卡住          | 已完成 | 单点/传统多点仍使用 `move_base` action；默认演示新增中心线跟踪，按连续中心线平滑过弯，实测完整一圈成功 |
| 不用 TurtleBot 默认模型，切回并优化自研小车 | 已完成 | 默认模型为 `raicom_guard_rover`；四轮 skid-steer 结构，只复用开源轮胎/LDS 网格，不套用 TurtleBot 整车                                  |
| 摄像头正对内部墙壁，RViz 可看数据           | 已完成 | 左侧嵌入式摄像头发布 `/camera/rgb/image_raw`，RViz 已增加 `Side Camera` 显示，摄像头不超过车身左右边界                                 |

## 10 项功能完成情况

| 编号 | 验收项                                 | 当前状态                     |
| ---- | -------------------------------------- | ---------------------------- |
| 1    | Gazebo 场地能正常打开                  | 已完成                       |
| 2    | 机器人能在 Gazebo 中正常显示           | 已完成，自研四轮车默认显示   |
| 3    | 机器人能通过键盘控制移动               | 已完成，`/cmd_vel` 链路实测  |
| 4    | 激光雷达话题 `/scan` 正常发布          | 已完成                       |
| 5    | SLAM 能正常建图                        | 已完成                       |
| 6    | 地图能保存为可复用文件                 | 已完成                       |
| 7    | 导航能让机器人到达指定点               | 已完成                       |
| 8    | 导航能连续到达多个指定点               | 已完成；默认中心线完整一圈，传统多点 23/23 可备用 |
| 9    | 到达识别目标附近后终端打印识别结果     | 已完成                       |
| 10   | 优化模型、美化场地、增加摄像头识别能力 | 已完成基础版                 |

## 注意事项与后续优化

- 当前场地按你后续要求保留墙体赛道、起点斑马线和横杠地面贴印，暂不加入垃圾桶、人群、楼宇实体模型。
- 自研车模型已经明显区别于 TurtleBot3，当前为四轮 skid-steer 结构；后续若追求更高外观分，可以继续导入自制 `.dae/.stl` 一体车壳。
- 默认中心线跟踪最新复测成功，机器人沿 297 点连续中心线完整绕场一圈并回到起点；传统多点导航 23/23 成功，作为备用演示方式保留。
- 可以在墙体上贴照片供摄像头识别。推荐做法是把 JPG/PNG 放进包内模型资源目录，用 Gazebo 材质脚本或薄 `box/plane` visual 贴到墙面；贴片只作为 visual，不加 collision，并从墙面外偏移约 0.005m，避免与墙面闪烁。为了识别稳定，建议优先使用高对比度照片、二维码、AprilTag 或颜色标志，贴片朝向侧向摄像头并保证光照充足。
