# 睿抗平安城市仿真任务完成报告

更新时间：2026-05-15

## 当前结论

当前工程默认使用自研四轮安防巡逻车 `raicom_guard_rover`，可启动 Gazebo、RViz、地图、AMCL、move_base、中心线巡航和摄像头真实模型识别。

识别链路已经从“位置触发/照片比对”改为真实模型推理：

- 摄像头输入：`/camera/rgb/image_raw`
- 模型来源：`/home/zcy/raicom_ws/2025源码/`
- ONNX 推理：`onnxruntime`
- 楼宇 OCR：默认关闭，楼宇名称使用通用“楼宇”，避免 FastDeploy/Paddle OCR 在 ROS 图像回调中段错误
- 默认识别节点：`camera_model_recognition_node.py`
- 旧版位置触发识别：保留为调试开关，默认不启动
- 识别输出已加入目标离开重置、间隔复报和目标面积占比过滤，避免楼宇提前输出后真正到楼宇处不再输出
- 中心线巡航已提速，同时加强航向和横向误差纠偏，尽量保证小车不明显偏离中线

## 启动前环境

每次新终端先执行：

```bash
source /opt/ros/noetic/setup.bash
source ~/raicom_ws/devel/setup.bash
```

已安装的关键 Python 依赖：

```bash
python3 -m pip install --user onnxruntime-gpu==1.19.2 fastdeploy-python nvidia-curand-cu12 nvidia-cufft-cu12
```

检查依赖：

```bash
python3 -c "import onnxruntime, cv2, numpy; print(onnxruntime.get_available_providers())"
```

## 常用命令

默认演示，启动 Gazebo、RViz、导航、中心线巡航和真实模型识别：

```bash
source /opt/ros/noetic/setup.bash
source ~/raicom_ws/devel/setup.bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true
```

只启动仿真和导航，不自动巡航：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true run_centerline_follower:=false run_multi_goal_patrol:=false
```

只启动摄像头真实模型识别节点：

```bash
roslaunch raicom_safe_city camera_recognition.launch
```

使用旧版位置触发识别调试：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true run_proximity_recognition:=true run_camera_recognition:=false
```

使用新保存地图导航：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true map_file:=/home/zcy/raicom_ws/src/raicom_safe_city/maps/my_slam_map.yaml
```

SLAM 建图：

```bash
roslaunch raicom_safe_city slam_mapping.launch rviz:=true
```

保存地图：

```bash
rosrun map_server map_saver -f ~/raicom_ws/src/raicom_safe_city/maps/my_slam_map
```

键盘控制：

```bash
roslaunch raicom_safe_city keyboard.launch
```

重新编译：

```bash
cd ~/raicom_ws
catkin_make
source devel/setup.bash
```

## 真实识别模型

当前节点调用这些模型：

| 任务         | 模型                                  | 推理          |
| ------------ | ------------------------------------- | ------------- |
| 垃圾桶       | `2025源码/垃圾桶/moxing/best.onnx`    | `onnxruntime` |
| 人群         | `2025源码/人/human.onnx`              | `onnxruntime` |
| 楼宇         | `2025源码/楼宇火灾识别/building.onnx` | `onnxruntime` |
| 火灾/楼层    | `2025源码/楼宇火灾识别/fire.onnx`     | `onnxruntime` |
| 楼宇文字 OCR | `2025源码/楼宇火灾识别/OCR/`          | 已保留接口，默认关闭 |

红绿灯模型也存在：

```text
2025源码/红绿灯/moxing/best.onnx
```

但当前平安城市默认输出不需要红绿灯结果，所以未接入默认节点。

## 识别输出格式

终端只输出比赛需要的结果：

```text
识别结果：垃圾桶类型=...
识别结果：人群类型=...，数量=...
识别结果：楼宇灾害情况=...
```

不输出“正在前往”“到达目标点”“目标编号”等导航过程信息。

## 主要文件

| 用途             | 文件                                                            |
| ---------------- | --------------------------------------------------------------- |
| 默认入口         | `src/raicom_safe_city/launch/gui_demo.launch`                   |
| Gazebo 入口      | `src/raicom_safe_city/launch/gazebo.launch`                     |
| 导航入口         | `src/raicom_safe_city/launch/navigation.launch`                 |
| 相机识别入口     | `src/raicom_safe_city/launch/camera_recognition.launch`         |
| 真实模型识别节点 | `src/raicom_safe_city/scripts/camera_model_recognition_node.py` |
| 中心线巡航       | `src/raicom_safe_city/scripts/centerline_follower.py`           |
| 旧版位置识别     | `src/raicom_safe_city/scripts/proximity_recognition_node.py`    |
| 自研机器人       | `src/raicom_safe_city/urdf/raicom_guard_rover.urdf.xacro`       |
| Gazebo 世界      | `src/raicom_safe_city/worlds/safe_city.world`                   |
| 默认地图         | `src/raicom_safe_city/maps/safe_city_map.yaml`                  |

## 已验证

已完成以下验证：

```bash
python3 -m py_compile src/raicom_safe_city/scripts/camera_model_recognition_node.py
xmllint --noout src/raicom_safe_city/launch/gui_demo.launch src/raicom_safe_city/launch/camera_recognition.launch
catkin_make
roslaunch raicom_safe_city camera_recognition.launch
roslaunch raicom_safe_city gui_demo.launch --nodes
```

验证结果：

- `onnxruntime-gpu` 可加载 2025 源码中的垃圾桶、人群、楼宇、火灾 ONNX 模型。
- 识别节点已确认使用 GPU：`ONNXRuntime Providers: ['CUDAExecutionProvider', 'CPUExecutionProvider']`。
- 2025 样例图片离线推理成功：
  - 垃圾桶检测到 `可回收物_未投放`
  - 人群检测到 `普通行人`、`职业人员`
  - 楼宇检测到 `楼宇`
  - 火灾楼层模型检测到 `火焰` 和多个楼层框
- 向 `/camera/rgb/image_raw` 发布三张测试图像后，终端已输出：
  - `识别结果：人群类型=普通行人，数量=1`
  - `识别结果：垃圾桶类型=厨余垃圾桶，状态=未投放`
  - `识别结果：楼宇灾害情况=楼宇2楼着火了`
- `camera_recognition.launch` 可稳定启动真实模型识别节点。
- headless 完整链路短启动通过，`camera_model_recognition` 与 Gazebo、导航可同场启动，不再出现 `exit code -11`。

2026-05-15 修复楼宇提前输出：

- 原因 1：旧逻辑中每类结果第一次打印后会永久去重，提前误识别楼宇后，真正到楼宇处不会再打印。
- 原因 2：旧楼宇逻辑在没有楼宇框时也会对整张画面跑火灾模型，可能被其他画面误触发。
- 现在楼宇必须先有足够大的楼宇框才输出；目标离开视野若干帧后会重置；同一目标持续可见时可按 `report_cooldown` 间隔复报。
- 对垃圾桶、人群、楼宇都增加面积占比过滤，三张仿真墙面图验证结果为：人群图只输出人群，垃圾桶图只输出垃圾桶，楼宇图只输出楼宇灾害。

2026-05-15 巡航提速：

- `gui_demo.launch` 中 `centerline_follower` 参数调整为：
  - `base_speed=0.220`
  - `corner_speed=0.130`
  - `lookahead=0.30`
  - `max_angular=1.80`
  - `heading_gain=2.85`
  - `cross_track_gain=0.55`
  - `max_cross_track_for_full_slowdown=0.13`
- `centerline_follower.py` 默认值同步更新，避免单独启动脚本时仍使用旧慢速参数。
- 设计意图：直线明显加速，弯道仍限速；缩短前视距离并提高角速度/纠偏增益，让车偏离中线后更快回正。
- headless 实跑验证：
  - 终端输出：`垃圾桶`、`人群`、`楼宇检测到火灾`
  - `centerline_follower` 正常完成一圈
  - 45 秒 odom 监控：最大中心线偏差约 `0.054m`，平均偏差约 `0.017m`
  - Gazebo 强制关闭时仍可能出现 `boost::lock_error`，属于退出阶段问题，不影响巡航和识别过程

说明：FastDeploy 1.0.7 内置的 ONNXRuntime 对部分 IR 9 ONNX 模型不兼容，所以当前 ONNX 检测模型使用独立 `onnxruntime` 推理。FastDeploy/PPOCR 在 ROS 实时图像回调里曾触发 `exit code -11`，因此默认关闭 OCR，保证演示稳定。

## RViz 卡顿处理

RViz 卡顿主要来自 WSL 图形转发、Gazebo GUI、RViz 图像显示、代价地图显示和模型推理同时运行。

已做优化：

- GPU 推理已启用，相机 Gazebo 发布频率恢复到 15Hz。
- ONNXRuntime 每个模型限制为单线程，避免抢占全部 CPU。
- RViz 默认帧率从 30 降到 10。
- RViz 默认关闭 TF 显示和 Global/Local Costmap 显示。

需要看代价地图时，可在 RViz 左侧 Displays 手动勾选 `Global Costmap` 和 `Local Costmap`。

## GPU 状态

当前默认启用 GPU 推理：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true
```

启动日志中看到下面内容说明已经真正用上 GPU：

```text
ONNXRuntime Providers: ['CUDAExecutionProvider', 'CPUExecutionProvider']
```

如果需要临时退回 CPU，可用：

```bash
roslaunch raicom_safe_city gui_demo.launch rviz:=true use_gpu:=false
```

## 后续重点

现场演示时主要确认两件事：

1. RViz 的 `Side Camera` 能看到墙面照片。
2. 小车巡航经过照片时，终端能打印三类 `识别结果：...`。

当前 `camera_model_recognition` 的识别频率为 `3.0Hz`。如果现场仍卡，可临时用 `use_gpu:=false` 或把 `process_hz` 调低。
